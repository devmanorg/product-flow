name: Задание на «…»

intro: |
  …

profits: # 😀 Зачем нужен продукт
  ${Роль пользователя}:
    - P1. …:
      - P1a. …
      - P1b. …
    - P2. …
  ${Роль пользователя}:
    - P3. …

competitors: # 🥊 Другие способы решить заявленные проблемы
  - …

referencies: # ∼ Примеры похожих продуктов
  - …

glossary:  # 📖 Используемые термины
  ${термин}:
    definition: ${короткое определение}
    actual_examples:  # Как оно работает сейчас
      ${заголовок}: ${подробный пример}
      …
    extension_examples:  # Возможные расширения понятия в будущем
      ${заголовок}: ${подробный пример}
      …

boundaries: # 🚫 Чего делать не будем
  ${название} → ${выгода}: |
    ${описание}    

risks: # 💣 Риски, которыми надо управлять: мониторить, подстраховываться и иметь готовый план действий
  - R1. ${событие} → ${ущерб владельцу продукта}: |
    ${описание}
  - R1. ${событие} → ${ущерб владельцу продукта}: |
    ${описание}

job_stories: # 👥 Как пользоваться продуктом
  # Только самые важные истории, без которых владелец продукта не примет работу
  ${Роль}:
    - ☐ A1. (риск A) ${событие} → Хочу ${желание} → ${акцептор действия}
    - ☑ A2. …

  ${Роль}:
    - ☐ B1. (риск A) ${событие} → Хочу ${желание} → ${акцептор действия}
    - ☑ B2. …

how_to_check: # ✅ Как проверить, что продукт готов. Всё что важно, но не влезает в формат job stories
  - ☐ T1. (риск A, B) ${что сделано для проверки}
  - ☑ T2. …

maintenance_report_template:  # 👷 Шаблон отчёта о поддержке продукта
  - ☐ (риск R2) ${что сделано для проверки}
  - ☐ …
