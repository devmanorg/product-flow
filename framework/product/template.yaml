продукт: ${Название итогового продукта}
точка зрения: Конечные пользователи
формат yaml-задания: v0.3

введение: |
  …

польза: # 😀 Польза от продукта. Зачем кому-то нужен продукт?
  P1. ${Глагол} ${объект} ${контекст}:
    кто: ${Роль} 
    старые способы решить проблему: # Существующие сервисы-аналоги, в т.ч. как пользователь решал свою проблему до появления разрабатываемого продукта
      - ${способ решения проблемы}: [@P1a, @P1b, ${преимущество старого способа}, …]
      - ${способ решения проблемы}: []
      - …
    фичи: # Ключевые особенности продукта
      - P1a. ${Глагол} ${объект}
      - P1b. ${Глагол} ${объект} ${контекст}
      - P1c. @unusable ${Глагол} ${объект}  # Отметьте выход за границу применимости тэгом @unusable
      - P1d. @extension ${Глагол} ${объект}  # Отметьте отложенный функционал тэгом @extension
      - …

  P2. @extension ${Глагол} ${объект} ${контекст}: # Отметьте отложенный функционал тэгом @extension 
    кто: ${Роль} (косвенно) # Отметьте, если роль не использует продукт напрямую
    старые способы решить проблему: # Существующие сервисы-аналоги, в т.ч. как пользователь решал свою проблему до появления разрабатываемого продукта
      - ${способ решения проблемы}: [@P2a, @P2b, ${преимущество старого способа}, …]
      - ${способ решения проблемы}: []
      - …
    фичи: # Ключевые особенности продукта
      - P2a. ${Глагол} ${объект}
      - P2b. ${Глагол} ${объект} ${контекст}
      - …

риски: # 💣 Риски, которыми надо управлять: мониторить, подстраховываться и иметь готовый план действий
  R1. @P1 ${название риска}:
    событие: ${описание события}
    последствия:
      - пострадавший: ${Кто страдает}
        почему: ${Как событие привело к ущербу}
        ущерб: ${Какой ресурс теряет}, ${формула расчета ущерба}, @{частота}

      - пострадавший: ${Кто страдает}
        почему: ${Как событие привело к ущербу}
        ущерб: ${Какой ресурс теряет}, ${формула расчета ущерба}, @{частота}

  R2. @P1 ${название риска}:
    …

истории: # 👥 Самые важные истории, без которых владелец продукта не примет работу
  ${Роль}:
    - A1. @P1a @R1 ${Глагол} ${объект} ${контекст}:
      - ☐ A1a ${Событие}: ${акцептор}
      - ☐ A1b ${Событие}: ${акцептор}
      - ☑ …
    - …

  ${Роль}:
    - ☐ B1. @P1b @R1 ${Глагол} ${объект} ${контекст}:
      - ☐ B1a ${Событие}: ${акцептор}
      - ☑ …
    - …

как проверить: # ✅ Как проверить, что продукт готов. Всё что важно, но не влезает в формат job stories
  - ☐ T1. @P1a @R1 ${что сделано для проверки}
  - ☑ …
