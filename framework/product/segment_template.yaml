сегмент:
  название:
    - ${название}
    - ${название внешнего сегмента}
    - ${название внешнего сегмента}]
  продукт:
    - ${название}
    - ${название внешнего продукта}
    - ${название внешнего продукта}

формат yaml-задания: v0.3

польза сегмента:
  - @P1 ${Роль} - ${Глагол} ${объект} ${контекст}
  - @P2a ${Роль} - ${Глагол} ${объект} ${контекст}
  - @P2b ${Роль} - ${Глагол} ${объект} ${контекст}

… # TODO остальное содержимое как в обычном задании
